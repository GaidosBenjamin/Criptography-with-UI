<Window x:Class="WpfApp1.InputKey"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="InputKey" Height="135" Width="400">
    <StackPanel Margin="10">
        <TextBlock Text="Input key for decryption" FontWeight="Bold" Margin="0 0 0 5"/>
        <TextBox x:Name="CustomKeyTextBox" Margin="0 0 0 10"/>

        <Grid Margin="0 0 0 10">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button IsDefault="true" x:Name="Ok" Click="Ok_Click" Grid.Column="0" Content="Ok" Margin="50 0 10 0"/>
            <Button IsCancel="True"  Grid.Column="1" Content="Cancel" Margin="10 0 50 0"/>

        </Grid>
    </StackPanel>
</Window>
